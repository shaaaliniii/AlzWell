<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Family Tree</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <style>
        /* General Styles */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #74b9ff, #81ecec);
            background-attachment: fixed;
            background-size: cover;
            color: #333;
            animation: fadeIn 1s ease-in-out;
        }

        .container {
            max-width: 1200px;
        }

        h1 {
            font-weight: bold;
            color: white;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.6);
            animation: slideInDown 1s ease-out;
        }

        .btn-primary {
            background-color: #0984e3;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        /* Card Styles */
        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            animation: fadeInUp 0.8s ease-in-out;
        }

        .card:hover {
            transform: translateY(-15px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.3);
        }

        .card-img-top {
            height: 200px;
            object-fit: cover;
            border-radius: 10px 10px 0 0;
        }

        .card-title {
            font-size: 1.3rem;
            color: #333;
        }

        .card-text {
            color: #555;
        }

        .add-member-card {
            background: linear-gradient(135deg, #81ecec, #74b9ff);
            color: white;
            text-align: center;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
        }

        .add-member-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.3);
        }

        .add-member-card i {
            font-size: 50px;
            margin-bottom: 10px;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes slideInDown {
            from {
                transform: translateY(-50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                transform: translateY(50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        /* Footer */
        footer {
            margin-top: 30px;
            text-align: center;
            color: white;
            font-size: 0.9rem;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }

        /* Home Button Styles */
        .home-btn {
            position: absolute;
            top: 20px;
            left: 20px;
            background-color: #0984e3;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            color: white;
            transition: background-color 0.3s ease;
        }

        .home-btn:hover {
            background-color: #74b9ff;
        }
    </style>
</head>
<body>
    <!-- Home Button -->
    <button class="home-btn" onclick="window.location.href='/'">Home</button>

    <!-- Main Container -->
    <div class="container mt-5">
        <h1 class="text-center mb-4">Family Tree</h1>
        <div class="row">
            <% if (familyTree.length === 0) { %>
                <p class="no-family-text">No family members added yet. Click "Add Family Member" to get started!</p>
            <% } else { %>
                <% familyTree.forEach(member => { %>
                    <div class="col-md-4 col-lg-3 mb-4">
                        <div class="card text-center">
                            <img src="<%= member.photo %>" class="card-img-top" alt="<%= member.name %>">
                            <div class="card-body">
                                <h5 class="card-title"><%= member.name %></h5>
                                <p class="card-text"><strong>Relation:</strong> <%= member.relation %></p>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } %>
            <div class="col-md-4 col-lg-3 mb-4">
                <div class="add-member-card" onclick="window.location.href='/add-member'">
                    <i class="bi bi-plus-circle"></i>
                    <h5>Add New Member</h5>
                    <p>Click here to add a new family member</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
